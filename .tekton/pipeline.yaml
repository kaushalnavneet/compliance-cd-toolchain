apiVersion: tekton.dev/v1alpha1
kind: Pipeline
metadata:
  name: cd-pipeline
spec:
  params:
    - name: configurationContext
      description: Name of the configmap containing the pipeline context
      default: cd-context
    - name: configurationSecrets
      description: name of the configmap containing the pipeline context secrets
      default: cd-secrets  
    - name: repository
      description: the git repo
    - name: revision
      description: the branch for the git repo
      default: master
    - name: image
      description: the image to be deployed
    - name: changeRequestNumber
      description: ServiceNow CR number
      default: "notAvailable"
  tasks:
    - name: pipeline-deploy-task
      taskRef:
        name: deploy-task
      params:
        - name: configurationContext
          value: $(params.configurationContext) 
        - name: configurationSecrets
          value: $(params.configurationSecrets)
        - name: repository
          value: $(params.repository)
        - name: revision
          value: $(params.revision)
        - name: image
          value: $(params.image)
        - name: changeRequestNumber
          value: $(params.changeRequestNumber)